{
  "name": "body-battery-to-slack-status",
  "version": "0.0.0",
  "description": "",
  "bin": "build/src/index.js",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "@types/node": "^10.0.3",
    "@types/puppeteer": "^2.0.1",
    "gts": "^1.1.2",
    "ts-node": "^8.6.2",
    "typescript": "~3.7.0"
  },
  "dependencies": {
    "puppeteer": "^2.1.1"
  }
}
